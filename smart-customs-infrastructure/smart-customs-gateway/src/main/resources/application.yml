# ============================================================
# Gateway 服务个性化配置
# 共享配置(Redis/Sa-Token等)来自 Nacos: application-common.yml
# ============================================================

server:
  port: 9999
