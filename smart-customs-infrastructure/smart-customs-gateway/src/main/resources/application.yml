# ============================================================
# Gateway 服务个性化配置
# 共享配置(Redis/Sa-Token等)来自 Nacos: application-common.yml
# ============================================================

server:
  port: 9999

# 路由与发现配置已迁移至 Nacos (smart-customs-gateway.yml)

# Gateway 日志级别
logging:
  level:
    io.github.yak33.gateway: debug
    org.springframework.cloud.gateway: info
